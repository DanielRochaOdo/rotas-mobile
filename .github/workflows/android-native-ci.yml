name: Android Native CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  build-debug:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-kotlin
    env:
      SUPABASE_URL: https://example.supabase.co
      SUPABASE_ANON_KEY: ci-placeholder-key

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Allow Gradle wrapper execution
        run: chmod +x gradlew

      - name: Build native debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Ensure exact native root is active
        run: |
          if ! grep -q 'ExactRotasRoot' app/src/main/java/com/odontoart/rotas/MainActivity.kt; then
            echo 'MainActivity is not using the exact-parity native root.'
            exit 1
          fi
          if grep -Eq '^[[:space:]]*RotasRoot\(\)' app/src/main/java/com/odontoart/rotas/MainActivity.kt; then
            echo 'Legacy simplified RotasRoot is active in MainActivity.'
            exit 1
          fi

      - name: Ensure WebView is not used by Kotlin sources
        run: |
          if grep -R --line-number --include='*.kt' 'android.webkit.WebView\|WebViewAssetLoader\|JavascriptInterface' app/src/main/java; then
            echo 'WebView/web bridge usage found in native Android sources.'
            exit 1
          fi

      - name: Ensure Android build does not depend on web dist
        run: |
          if grep -R --line-number 'syncWebAssets\|../dist\|npm run build' app/build.gradle.kts; then
            echo 'Web build dependency found in Android Gradle configuration.'
            exit 1
          fi
